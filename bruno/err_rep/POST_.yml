info:
  name: /
  type: http
  seq: 3

http:
  method: POST
  url: "{{endpoint}}/err_rep?project_name={{project_name}}"
  params:
    - name: project_name
      value: "{{project_name}}"
      type: query
  body:
    type: form-urlencoded
    data:
      - name: payload
        value: JS error at line 42
  auth: inherit

runtime:
  actions:
    - type: set-variable
      phase: after-response
      selector:
        expression: res.body.captcha_passed_token
        method: jsonq
      variable:
        name: captcha_passed_token
        scope: runtime

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
